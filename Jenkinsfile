pipeline {
    agent any

    stages {
        stage('Run Python') {
            steps {
                // Run the Python code
               bat 'python script.py'
            }
        }
        
    }
}
